namespace Ettad.Data.Enums
{
    /// <summary>Weapon caliber category (Small / Medium / Large). Numeric values match <see cref="AmmunitionType"/> for alignment.</summary>
    public enum WeaponCaliberCategory
    {
        Small = 1,
        Medium = 2,
        Large = 3
    }
}
